// Minimal, secure preload. Nothing is exposed yet — the overlay talks to the
// app's own /api routes over HTTP. Add a contextBridge API here later if you
// want native features (e.g. notifications, position control).
window.addEventListener('DOMContentLoaded', () => {
  // place for future bridges
})
